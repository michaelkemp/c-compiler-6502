"""Memory-mapped I/O devices (Phase 2 -- not yet implemented).

Will hold the minimal console device described in docs/architecture.md
(output register + input status/data), attached into the Bus's I/O region.
"""
